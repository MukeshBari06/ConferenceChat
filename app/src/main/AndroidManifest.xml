<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.back4app.quickstartexampleapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherlg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherlg_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditProfile" />
        <activity android:name=".UserImages" />
        <activity android:name=".sharePhotos" />
        <activity android:name=".ChatActivity2" />
        <activity
            android:name=".UserListView"
            android:theme="@style/CustomTheme" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.parse.SERVER_URL"
            android:value="@string/server_url" />
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="@string/app_id" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="@string/client_key" />
    </application>

</manifest>